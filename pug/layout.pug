block vars
    - var page = { title: "Title", subtitle: "Subtitle", author: "author", description: "description", tags: ["tag1", "tag2", "tag3"] }

doctype html
html
    include includes/head.pug

    body(class=page.title, class=page.bodyclass).has-sidenav
        header
            b UnHack for Jekyll
            include includes/topnav.pug 
        .container
            block content
        block additional

        include includes/sidenav.pug 
        
        include includes/sitesettings.pug
        include includes/appsettings.pug
        
        include includes/footer.pug
    // body end
// html end
