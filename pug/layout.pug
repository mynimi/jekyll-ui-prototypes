block vars
    - var page = { title: "Title", subtitle: "Subtitle", author: "author", description: "description", tags: ["tag1", "tag2", "tag3"] }

doctype html
html(lang="de")
    include includes/head.pug

    body(class=page.title)
        header
            b UnHack for Jekyll
            include includes/topnav.pug 
        .container
            block content
        block additional

        include includes/footer.pug
    // body end
// html end
